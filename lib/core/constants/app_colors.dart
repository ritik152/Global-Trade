import 'package:flutter/material.dart';

class AppColors {
  // Primary (Deep Navy / Dark Blue)
  static const Color primary = Color(0xFF0F2C59);
  static const Color primaryLight = Color(0xFF1E488B);
  static const Color primaryDark = Color(0xFF071731);

  // Secondary (White / Off-white)
  static const Color secondary = Color(0xFFF8F9FA);
  
  // Accent (Refined Gold/Orange)
  static const Color accent = Color(0xFFE8AA42);
  static const Color accentLight = Color(0xFFF3C77D);
  static const Color accentDark = Color(0xFFB88229);

  // Backgrounds
  static const Color background = Color(0xFFFFFFFF);
  static const Color surface = Color(0xFFF8F9FA);
  static const Color darkBackground = Color(0xFF081225);

  // Text Colors
  static const Color textPrimary = Color(0xFF1A1A1A);
  static const Color textSecondary = Color(0xFF6C757D);
  static const Color textLight = Color(0xFFFFFFFF);
  static const Color textLightSecondary = Color(0xFFD0D0D0);

  // Status
  static const Color success = Color(0xFF28A745);
  static const Color error = Color(0xFFDC3545);
  
  // Borders & Dividers
  static const Color border = Color(0xFFE9ECEF);
}
